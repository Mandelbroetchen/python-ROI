[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"  # ← not setuptools.backends.legacy:build

[project]
name = "my-package"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = []